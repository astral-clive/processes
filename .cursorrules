# AI Development Guidelines

## Plugin/Extension Development

When a user asks to "build an extension", "build a plugin", "add a feature", or similar:

### Critical Rules (MUST FOLLOW)

1. **Location**: All plugins MUST be placed in the `plugins/` directory
   - ❌ WRONG: `plugins/my-plugin.tsx` AND `plugins/my-plugin-README.md` (loose files)
   - ✅ CORRECT for single-file: `plugins/my-plugin.tsx` (ONLY if truly single file with no docs)
   - ✅ CORRECT for multi-file: `plugins/my-plugin/index.tsx` + `plugins/my-plugin/README.md`
   
2. **Directory Structure (CRITICAL)**: 
   - **If a plugin has MORE THAN ONE FILE** (including README, types, helpers, etc.):
     - ✅ MUST create subdirectory: `plugins/my-plugin/`
     - ✅ Entry point MUST be `index.tsx`
     - ✅ ALL related files MUST be inside that directory
   - **Only use loose files for true single-file plugins with no documentation**
   
3. **DO NOT modify files outside `plugins/`**: 
   - ❌ DO NOT edit `src/` directory
   - ❌ DO NOT edit core components (`ProcessNode.tsx`, `ProcessEdge.tsx`, etc.)
   - ❌ DO NOT edit `vite.config.ts`, `package.json`, or other config files
   - ✅ ONLY create/modify files within `plugins/` directory

4. **Self-Contained**: If a plugin needs multiple files, services, or APIs:
   - Create a subdirectory: `plugins/my-plugin/`
   - Place ALL related files in that directory
   - Example structure:
     ```
     plugins/my-plugin/
       ├── index.tsx          # Main plugin entry point (REQUIRED)
       ├── README.md          # Plugin documentation
       ├── components/        # Plugin-specific components
       ├── services/          # Plugin-specific services/APIs
       ├── utils/             # Plugin-specific utilities
       └── types.ts           # Plugin-specific types
     ```

5. **File type**: Plugin entry points must be `.tsx` files (auto-discovered on startup)

6. **Hook System Only**: Use the hook system - this is the ONLY way to extend functionality

7. **Documentation**: Read `plugins/README.md` for complete technical reference

### Quick Plugin Template

```typescript
import { hooks } from '@/lib/hooks'
import type { Plugin } from '@/lib/plugins'
import type { HeaderButtonHookContext } from '@/lib/hook-types'

function initialize() {
  // Register hooks here
  hooks.addAction<HeaderButtonHookContext>('header:viewMode:buttons', ({ doc }) => {
    return (
      <button className="icon-button" onClick={() => alert('Hello!')}>
        My Button
      </button>
    )
  })
}

const plugin: Plugin = {
  name: 'My Plugin',
  version: '1.0.0',
  initialize
}

export default plugin
```

### Available Hooks

- `header:viewMode:buttons` - Add buttons to header (view mode)
- `header:editMode:buttons` - Add buttons to header (edit mode)  
- `inspector:node:fields` - Add fields to node inspector
- `inspector:edge:fields` - Add fields to edge inspector

See `src/lib/hook-types.ts` for detailed hook definitions and context types.

### Important: Capabilities and Limitations

**What Plugins CAN Do:**
- ✅ Add UI elements (buttons, form fields) via hooks
- ✅ Modify data properties on nodes/edges
- ✅ Add custom data fields to nodes/edges
- ✅ Create subdirectories within `plugins/` for organization
- ✅ Include helper files, services, APIs within their plugin directory
- ✅ Use external npm packages (already in package.json)

**What Plugins CANNOT Do:**
- ❌ Modify files outside `plugins/` directory
- ❌ Change node/edge visual appearance (shape, layout, graphics)
- ❌ Modify React components (`ProcessNode.tsx`, `ProcessEdge.tsx`)
- ❌ Add custom node types to ReactFlow
- ❌ Modify core application files or configuration
- ❌ Change Vite configuration or build process

**If a user requests something that requires modifying core files:**
- Explain that it requires core changes (outside plugin system)
- Suggest they modify core files manually
- Do NOT attempt to work around this limitation

### Examples

See `plugins/examples/` for reference implementations:
- `example-button-plugin.tsx` - Simple header button
- `example-inspector-plugin.tsx` - Simple inspector field
- `github-ticket-flow-extension.tsx` - Complete real-world example

## Project Architecture

- **Framework**: React 18 + TypeScript + Vite
- **Flowchart Engine**: ReactFlow
- **Styling**: Tailwind CSS
- **Data Storage**: File-based JSON in `data/` directory
- **Plugin System**: Hook-based with auto-discovery from `plugins/**/*.tsx`

## Key Files

- `plugins/README.md` - Complete plugin development guide (PRIMARY REFERENCE)
- `src/lib/hook-types.ts` - Hook type definitions and documentation
- `src/lib/plugins.ts` - Plugin loader with auto-discovery
- `src/lib/hooks.ts` - Hook registry implementation
- `docs/LAYOUT_RULES.md` - AI flowchart generation guidelines
- `docs/ARCHITECTURE.md` - System architecture overview

## Development Workflow

1. **Adding a simple plugin**: Create `.tsx` file in `plugins/`, it auto-loads
   - Example: `plugins/my-plugin.tsx`

2. **Adding a complex plugin**: Create subdirectory in `plugins/`
   - Example: `plugins/my-plugin/index.tsx` (entry point)
   - Include all related files in `plugins/my-plugin/` directory

3. **Removing a plugin**: Delete the file/directory or rename to `.tsx.disabled`

4. **Testing**: Restart dev server to reload plugins

5. **Debugging**: Check browser console for plugin load messages

## Plugin UI Styling

When adding form elements to inspector panels:

- **Text inputs**: Use `className="form-input"`
- **Labels**: Use `className="form-label"`
- **Checkboxes**: Use `className="form-toggle"`
- **Select dropdowns**: Use `className="form-input"` AND `style={{ colorScheme: 'dark' }}`

**CRITICAL for Select Elements**: Always add `style={{ colorScheme: 'dark' }}` to `<select>` elements to ensure dropdown options are visible against the dark UI. Without this, options may appear as white text on white background.

Example:
```tsx
<select
  className="form-input"
  style={{ colorScheme: 'dark' }}
  value={value}
  onChange={handleChange}
>
  <option value="option1">Option 1</option>
  <option value="option2">Option 2</option>
</select>
```

### Multi-File Plugin Example

```
plugins/
  └── github-integration/
      ├── index.tsx              # Plugin entry point (exports Plugin object)
      ├── components/
      │   ├── GitHubButton.tsx   # UI components
      │   └── IssueModal.tsx
      ├── services/
      │   └── github-api.ts      # API calls to GitHub
      ├── utils/
      │   └── markdown.ts        # Helper utilities
      └── types.ts               # TypeScript types
```

**Key Point**: Everything needed for the plugin is self-contained within its directory. No files outside `plugins/` are modified.

## Data Structure

Processes are stored as JSON files in `data/`:
- `data/categories.json` - Category and process listings
- `data/processes/{categoryId}-{processId}.json` - Individual process files

Each process file contains:
- Metadata (title, description, IDs)
- Nodes (with positions, titles, descriptions, colors, custom data)
- Edges (with connections, labels, styling, custom data)

Plugins can extend node/edge data objects with custom fields.

## Creating Flowcharts

When creating or modifying flowchart process files, **ALWAYS follow these rules**:

### Edge Connection Handles (CRITICAL)

**All edges MUST specify sourceHandle and targetHandle** for proper flow visualization:

- **Sequential flow (top-to-bottom)**: 
  ```json
  {
    "source": "node-a",
    "sourceHandle": "bottom",
    "target": "node-b",
    "targetHandle": "top"
  }
  ```

- **Horizontal branches**:
  - Right branch: `sourceHandle: "right"`, `targetHandle: "left"`
  - Left branch: `sourceHandle: "left"`, `targetHandle: "right"`

**Default behavior**: If handles are not specified, edges may connect inconsistently.

### Node Spacing and Layout

- **Vertical spacing**: 70-80px minimum gap between node edges
- **Horizontal spacing**: 300px+ between parallel branch nodes
- **Calculate positions dynamically** based on actual node heights
- **Avoid collisions**: Ensure nodes don't overlap
- **Follow flow patterns**: Sequential → Branch → Merge

### Required Reading

**Before creating any flowchart**, reference:
- `docs/LAYOUT_RULES.md` - Comprehensive layout guidelines
- `data/processes/alpha-onboarding-to-team.json` - Well-formed example

### Valid Node and Edge Properties

**CRITICAL**: When creating flowcharts, ONLY use these valid options from the inspector:

**Node Colors** (MUST be one of these 8 hex values):
- Blue: `#3b82f6` (default)
- Pink: `#fca5a5`
- Purple: `#c4b5fd`
- Green: `#86efac`
- Yellow: `#fde047`
- Orange: `#fdba74`
- Teal: `#5eead4`
- Lavender: `#e9d5ff`

**Edge Properties** (default values):
- `color`: `#94a3b8` (fixed, not editable)
- `lineStyle`: `solid` or `dashed` (UI only supports these two)
- `arrow`: `none` or `arrow` (UI checkbox)
- `branchStyle`: `default`, `positive`, `caution`, or `danger` (for styling branches)

**Example Node:**
```json
{
  "data": {
    "title": "Node Title",
    "description": "Node description",
    "color": "#3b82f6"
  }
}
```

**Example Edge:**
```json
{
  "data": {
    "label": "Optional label",
    "color": "#94a3b8",
    "lineStyle": "solid",
    "arrow": "arrow",
    "branchStyle": "default"
  }
}
```

### Quick Checklist

When generating a flowchart:
- [ ] All edges have `sourceHandle` and `targetHandle` properties
- [ ] Sequential flows use `bottom → top` handles
- [ ] Nodes have adequate spacing (70px+ vertical, 300px+ horizontal)
- [ ] No node overlaps or collisions
- [ ] Edge paths don't cross through unrelated nodes
- [ ] Node colors are ONLY from the 8 valid options above
- [ ] Edge properties use valid values (don't invent custom colors or styles)

